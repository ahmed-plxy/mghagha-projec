{
  "name": "mghagha-mobile",
  "version": "1.0.0",
  "description": "Mghagha Store Android App",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No frontend build needed — app wraps live server'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
