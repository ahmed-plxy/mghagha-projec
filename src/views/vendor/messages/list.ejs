<%- include('../../partials/header', { title: 'الرسائل' }) %>
<div class="container">
  <h1>الرسائل</h1>
  <% if (conversations.length === 0) { %>
    <p>لا توجد محادثات حتى الآن.</p>
  <% } else { %>
    <table class="data-table">
      <thead><tr><th>العميل</th><th>الهاتف</th><th>آخر رسالة</th><th></th></tr></thead>
      <tbody>
        <% conversations.forEach(function(c) { %>
          <tr>
            <td><%= c.customer_name %></td>
            <td><%= c.customer_phone %></td>
            <td><%= c.last_message_at %></td>
            <td><a href="/vendor/messages/<%= c.id %>" class="btn btn-small btn-secondary">فتح المحادثة</a></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</div>
<%- include('../../partials/footer') %>
