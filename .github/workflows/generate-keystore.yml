# This workflow has been removed.
# The Android mobile-app project was deleted from this repository.

name: '[DISABLED] Generate Keystore'

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "This workflow is disabled. The mobile-app project has been removed."
